import { Routes } from '@angular/router';
import { PublicHomePage } from '@app/public/home/public-home-page';

export const routes: Routes = [
    {
        title: 'Home',
        path: '',
        component:PublicHomePage,
        
    }
];
